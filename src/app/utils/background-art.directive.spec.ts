import { BackgroundArtDirective } from './background-art.directive';

describe('BackgroundArtDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundArtDirective();
    expect(directive).toBeTruthy();
  });
});
